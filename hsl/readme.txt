[*编译说明*]
进入platform/linux目录下
 >>> 设置 ./config.sh (仅在第一次编译单板版本时需要)
     export KERNEL_SOURCE=
     export COMPILER_DIR=
     export BROADCOM_SDK=
	 enable/disable 各编译开关
     执行  ./config.sh 
 >>> 执行  make all
